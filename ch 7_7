#include <cstdlib>
#include <iostream>
#include <cstring>
#include<fstream>
using namespace std;
int main() {
	int n, i;

	do {
		cout << "輸入一個正整數";
		cin >> n;
	} while (n <= 0);

	cout << n << "的因數有";

	for (i = 1; i <= n; i++) {
		if (n % i == 0) {
			cout << i << ' ';
		}
	}
	return 0;
}
