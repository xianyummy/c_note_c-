#include <cstdlib>
#include <iostream>
#include <string>
using namespace std;
int main() {
	int num;

	cin >> num;
	
	if (num < 0) {
		num = (-1) * num;
		cout << num;
	}
	else {
		cout << num;
	}
	return 0;
}
