#include <cstdlib>
#include <iostream>
#include <cstring>
#include<fstream>
using namespace std;
int main() {
	int n = 1, sum = 0;

	do {
		sum += n;
		n++;
	} while (sum < 1000);

	cout << "n = " << n - 1;

	return 0;
}
